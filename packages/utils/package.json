{
  "name": "@pkg/utils",
  "version": "0.0.0",
  "private": true,
  "description": "General utilities for Node and the browser",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup lib/index.ts --format=esm --clean --sourcemap --dts",
    "postinstall": "pnpm run build"
  },
  "dependencies": {
    "tailwind-merge": "^1.14.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/node": "^20.4.8",
    "tsup": "^7.2.0",
    "typescript": "^5.1.6"
  }
}
